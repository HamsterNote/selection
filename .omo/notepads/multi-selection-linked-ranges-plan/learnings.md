- Task 1: Added linked-mode public model types in `src/types.ts` without deleting or replacing legacy `SelectionRange`. `SelectionProps` now accepts optional linked props (`selectionId`, `linkedMode`, `linkedData`, linked callbacks) while legacy callers can continue omitting linked mode and using existing `ranges`/callbacks. `src/index.ts` re-exports every new linked public type.
- Task 2: Added `pixelRectsToPercentRects`, `percentRectsToPixelRects`, and `percentRectListsEqual` in `src/geometry.ts`, exported via `src/index.ts`. Linked controlled geometry remains typed as `Record<string, PercentOverlayRect[]>`; pixel `OverlayRect[]` is only for transient overlay/hit-test/handle/popover geometry. `npm run typecheck` passes.
- Task 3: Updated `src/Selection.tsx` so legacy mode still recomputes persisted SVG rects from `ranges`, while linked mode validates non-empty `selectionId`, filters `linkedData.items` by endpoint or `rectsBySelectionId[selectionId]`, converts only that selection's percent rects to transient pixel rects, and renders rects with `data-range-id` plus `data-selection-id`. Linked selected styling now reads `linkedData.selectedRangeId`; legacy selected styling still reads `selectedRangeId`. `npm run typecheck` passes.
- Task 4: Added `src/linkedRegistry.ts` and exported its helpers from `src/index.ts`. Linked `Selection` components now register their container by normalized `selectionId`, unregister on cleanup, ignore duplicate ids after a development warning, expose DOM-ordered registered containers, resolve native node/offset endpoints to local plain-text offsets, and keep `LinkedSelectionData.selectionOrder` synchronized through `onLinkedDataChange`. `npm run typecheck` passes and output is captured in `.omo/evidence/task-4-multi-selection-linked-ranges-plan.md`.
- Task 5: `useTextSelection` now has an optional linked path that resolves endpoints through `linkedRegistry`, clips the native Range to each registered container it intersects, persists only percent rects, and exposes one `LinkedSelectionRange` for confirmation. `Selection` linked mode appends exactly one linked item and calls linked callbacks, while same-container linked selections remain linked-shaped and may also fire representable legacy callbacks. `npm run typecheck` passes; LSP diagnostics tool was unavailable in-session.
- Task 6: Upgraded `src/Selection.tsx` for linked-mode handle behavior. `onLinkedUpdateRange` is now wired via a bridging ref (`onLinkedUpdateRangeRef`), and `linkedSelectionIdRef` lets the `[]`-dep `startHandleDrag` callback read the current linked selection id. `startHandleDrag` sets the drag anchor from the linked item's local offset for same-Selection items (both endpoints equal current `selectionId`) and sets anchor to `-1` for cross-Selection items, blocking their drag in pointermove. The pointermove effect has a linked branch that computes new `lo`/`hi` offsets, rebuilds the DOM Range, converts resulting pixel rects to percent rects via `pixelRectsToPercentRects`, updates the `LinkedSelectionRange` and `LinkedSelectionData`, and calls `onLinkedUpdateRange` + `onLinkedDataChange` — never `onUpdateRange`. The persisted handle rendering condition changed from `!linkedContext` to an inline same-Selection check: cross-Selection linked items have no handles. Hit testing (`handleContainerClick`) and popover anchoring already worked in linked mode because they use `persistedRects` and `currentSelectedRangeId` which are linked-aware. `npm run typecheck`, `npm run build`, and `npm run lint` all pass.
- Task 7: Refactored `demo/src/App.tsx` to demo multi-region linked-mode. Replaced the single-region `ranges`/`selectedRangeId` demo state with `overallData: LinkedSelectionData` (`{ items, selectedRangeId, selectionOrder }`). Two labeled Selection panels (`page-a` / `page-b`) render side-by-side, both with `linkedMode={true}`, `linkedData={overallData}`, `onLinkedDataChange={setOverallData}`, and linked callbacks. `onLinkedDataChange` is the primary state updater (appends items on highlight, immutably updates on drag); `onLinkedSelectRange` independently updates `selectedRangeId` for click-to-toggle and start-drag paths that don't pass through `onLinkedDataChange`. `onLinkedSelect` and `onLinkedUpdateRange` log events with linked item id and endpoints. Per-selection refs stored in `Record<string, SelectionRef | null>`; an `activeLinkedId` radio controls which panel the global highlight button targets. Existing controls (hideHandlesOnSelection, customHandleMode, colorPreset) wired to all panels. A checkbox toggle reveals a legacy-only Selection (no linkedMode/selectionId/linked props) using old flat `legacyRanges`/`legacySelectedId` state. List controls adapted: linked list reads from `overallData.items` and shows `selectionId` span labels; legacy list kept separately. Logs now include `source` tag (page-a/page-b/legacy) and `linked-select` / `linked-update` kinds. Both panels preserve relative-positioned inline content (footnotes, badges, nested sup spans) for coordinate testing. `npm run typecheck` and `npm run build:demo` both exit 0; evidence captured in `.omo/evidence/task-7-multi-selection-linked-ranges-plan.md`.
- Task 7 verification (re-run): `demo/src/App.tsx` already reflects the multi-region linked-mode refactor described above. `npm run typecheck` passes with no diagnostics, and `npm run build:demo` produces `dist-demo/index.html`, `dist-demo/assets/index-u1Htjasu.css`, and `dist-demo/assets/index-PBmtLPWJ.js` successfully. Evidence saved to `.omo/evidence/task-7-multi-selection-linked-ranges-plan.md`.
- Task 8: Added a collapsed JSON inspector panel at the bottom of `demo/src/App.tsx`, below the "联动高亮" list and "钩子事件日志" grid. Uses a native `<details>`/`<summary>` (collapsed by default) with a `<pre>` rendering `JSON.stringify(overallData, null, 2)` — pretty-printed (2-space indent), no truncation or minification. The JSON includes `items`, `selectedRangeId`, `selectionOrder`, and every per-item field (`id`, `text`, `start.selectionId`, `start.offset`, `end.selectionId`, `end.offset`, `createdAt`, `rectsBySelectionId`). Minimal inline styling applied (bg, border, padding, monospace, overflow auto, maxHeight 480px). `npm run typecheck` and `npm run build:demo` both pass; evidence saved to `.omo/evidence/task-8-multi-selection-linked-ranges-plan.md`.
- Task 9: Ran the compatibility, validation, and browser evidence pass. `mkdir -p .omo/evidence` completed first. `npm run typecheck`, `npm run build`, `npm run build:demo`, and `npm run lint` all exit 0 with outputs captured in `.omo/evidence/task-9-typecheck.md`, `.omo/evidence/task-9-build.md`, `.omo/evidence/task-9-build-demo.md`, and `.omo/evidence/task-9-lint.md`. Browser QA ran against `npm run dev` on `http://localhost:9536/` and generated `.omo/evidence/task-9-manual-qa.md` plus screenshots under `.omo/evidence/task-9-screenshots/`. QA confirmed same-Selection linked highlight creates one item; cross-Selection page-a→page-b creates exactly one linked item with `rectsBySelectionId` keys `page-a` and `page-b`; the required DOM query `document.querySelectorAll('svg rect[data-range-id="<id>"]')` returns rects with distinct `data-selection-id` values; clicking a `page-b` rect selects the whole item and selected styling appears on both segments; deleting removes the whole item and all visible rects; cross-Selection items render no draggable handles; the JSON panel is collapsed by default and pretty-prints required `overallData` fields; legacy mode still supports old `ranges` selection, selected styling, popover delete, and same-item handle behavior. No feature bugs or unrelated lint failures were found.
- F3: Performed an independent real-browser manual QA pass against `npm run dev` on `http://localhost:9536/` with Playwright Chromium at `1440x1100`. Evidence saved in `.omo/evidence/f3-manual-qa.md` with screenshots under `.omo/evidence/f3-screenshots/`. Verdict APPROVE: same-page linked highlight, cross-page linked highlight, `svg rect[data-range-id="<id>"]` data attributes, page-b rect click selection, no cross-item handles, cross-item deletion, expanded JSON fields, and legacy selection/delete/handle behavior all passed. No source changes were needed.
- F4 scope fidelity audit: Approved scope boundaries in `.omo/evidence/f4-scope-fidelity.md`. `package.json` matches `HEAD:package.json` with no dependency/devDependency/test-framework changes; import scan found no Vitest/Playwright/Testing Library/page-framework additions; changed files are limited to linked-range source/demo plus `.omo` planning/evidence artifacts. Legacy `SelectionRange` remains exported and used, linked controlled data persists `Record<string, PercentOverlayRect[]>`, and cross-Selection highlights append exactly one `LinkedSelectionRange` item rather than split top-level items.
- F1 plan compliance audit: REJECT. Evidence written to `.omo/evidence/f1-plan-compliance.md`. Most Must Have/Must NOT and success criteria pass, but linked cross-container capture persists `text: selection.toString()` in `src/useTextSelection.ts`, and Task 9 QA shows the cross item text includes non-linked interstitial Demo label text (`selectionId = "page-b"`). This violates the plan requirement to include only text/rect fragments inside registered linked containers when crossing non-linked content.
- F1 blocker fix: Updated only `src/useTextSelection.ts` so linked capture accumulates `fragment.toString()` from the DOM-ordered registered-container loop and uses that filtered text for both `LinkedSelectionRange.text` and transient `selectedText`. `npm run typecheck`, `npm run build`, and `npm run lint` pass with fix evidence. Browser QA using cached Chromium confirmed native selection still crosses the interstitial label, but `overallData.items[0].text` excludes `selectionId = "page-b"` while keeping `rectsBySelectionId` keys for both `page-a` and `page-b`. F1 evidence is updated to APPROVE.
- F2 code-quality review: APPROVE. Reviewed `src/types.ts`, `src/Selection.tsx`, `src/useTextSelection.ts`, `src/index.ts`, and `demo/src/App.tsx`; evidence saved to `.omo/evidence/f2-code-quality.md`. `npm run typecheck` and `npm run lint` pass. Anti-pattern grep found no `as any`, `@ts-ignore`, `@ts-expect-error`, `TODO`, `FIXME`, `console.log`, debugger, lint disables, or missing demo-used type exports. The only `console.warn` is a dev-only linkedMode misuse warning at `src/Selection.tsx:309`, which is appropriate. Known non-blocking debt: `src/Selection.tsx` and `demo/src/App.tsx` are oversized; `src/useTextSelection.ts` remains under 250 pure LOC.
