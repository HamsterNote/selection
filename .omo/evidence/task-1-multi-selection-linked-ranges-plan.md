# Task 1 typecheck evidence

Command: `npm run typecheck`

```text

> @hamster-note/selection@0.0.0 typecheck
> tsc -b --noEmit


(exit code: 0)
```

Note: legacy callers can omit linked mode props; existing SelectionProps legacy callbacks and ranges API remain present.
