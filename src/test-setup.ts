/**
 * 测试环境设置文件
 * 在每个测试文件运行前自动执行，用于配置测试环境
 */

// 导入 jest-dom 自定义匹配器
// 例如：expect(element).toBeInTheDocument()
import '@testing-library/jest-dom/vitest';

// 如果需要自定义测试环境配置，可以在此添加
// 例如：全局 mock、测试工具初始化等
